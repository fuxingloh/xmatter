{
  "name": "xmatter",
  "version": "0.0.0",
  "private": false,
  "type": "module",
  "files": [
    "**/*.d.ts",
    "**/*.d.ts.map",
    "**/*.js"
  ],
  "scripts": {
    "build": "tsc",
    "clean": "tsc --build --clean"
  },
  "lint-staged": {
    "*": [
      "prettier --write --ignore-unknown"
    ]
  },
  "dependencies": {
    "zod": "^4.3.5"
  }
}
