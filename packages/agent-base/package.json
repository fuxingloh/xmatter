{
  "name": "@workspace/agent-base",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "clean": "tsc --build --clean",
    "test": "vitest run"
  },
  "lint-staged": {
    "*": [
      "prettier --write --ignore-unknown"
    ]
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "xmatter": "workspace:*",
    "zod": "^4.3.5"
  }
}
