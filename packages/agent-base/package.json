{
  "name": "@workspace/agent-base",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "sync": "node dist/bin.js",
    "test": "vitest run"
  },
  "lint-staged": {
    "*": [
      "prettier --write --ignore-unknown"
    ]
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "clipanion": "3.2.1",
    "yaml": "^2.8.2"
  }
}
